# KP Integration Test Configuration
lms_base_uri=${lms_service_url}
lms_api_key=${lms_private_api_key}
lms_service_base_uri = ${lms_service_base_uri}

# User Auth Token Required to make api call?
user_auth_enable=${user_auth_enable}

# Auth Token Configuration
env_base_path=${env_base_path}
lms_creator_user=${lms_course_creator_password}
lms_creator_password=${lms_course_creator_password}
lms_reviewer_user=${lms_course_reviewer_password}
lms_reviewer_password=${lms_course_reviewer_password}

# Header Config
lms_test_default_channel=${lms_default_channel}
lms_test_default_appId=${lms_default_appId}

